# .env.example — Template biến môi trường cho local development
# Sao chép file này thành .env và điền giá trị thật
# KHÔNG commit file .env lên Git

# Lấy key tại: https://console.groq.com → API Keys → Create API Key
GROQ_API_KEY=gsk_your_api_key_here
